{{ ansible_managed | comment }}

{{ logs_folder }}/apt/term.log {
  rotate 12
  monthly
  compress
  missingok
  notifempty
}

{{ logs_folder }}/apt/history.log {
  rotate 12
  monthly
  compress
  missingok
  notifempty
}
