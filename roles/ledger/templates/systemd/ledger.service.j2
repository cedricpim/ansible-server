{{ ansible_managed | comment }}

[Unit]
Description=Ledger recurring expenses
OnFailure=status_email@%n.service

[Service]
ExecStart=/bin/bash -lc 'rledger'
User={{ ledger_user }}
Group={{ ledger_user }}
Type=oneshot
