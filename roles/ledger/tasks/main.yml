---
- name: "Create {{ ledger_user }} user"
  user:
    name: "{{ ledger_user }}"
    home: "{{ ledger_home }}"
    shell: "/bin/false"
    state: "present"

- import_tasks: ledger.yml
- import_tasks: mariadb.yml
