{{ ansible_managed | comment }}

user  {{ nginx_user }};
worker_processes  1;

error_log  {{ nginx_log_directory }}/error.log warn;
pid        {{ nginx_pidfile }};

events {
  worker_connections  1024;
}

http {
  include       {{ nginx_config_directory }}/mime.types;
  default_type  application/octet-stream;

  log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

  access_log  /var/log/nginx/access.log  main;

  sendfile           on;

  keepalive_timeout  65;

  # More strict protocol permissions and ciphers
  ssl_dhparam  {{ nginx_ssl_dhparam }};
  ssl_ciphers  "{{ nginx_ssl_ciphers }}";
  ssl_protocols  {{ nginx_ssl_ciphers | join(" ") }};
  ssl_session_cache  {{ nginx_ssl_session_cache }};
  ssl_prefer_server_ciphers  on;

  # Set HTTP Strict Transport Security
  add_header  Strict-Transport-Security "max-age=63072000; includeSubdomains; ";
  add_header  X-Frame-Options "DENY";

  include {{ nginx_config_directory }}/conf.d/*.conf;
}
